[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["cd railway-api && pip install -r requirements.txt"]

[start]
cmd = "cd railway-api && uvicorn app.main:app --host 0.0.0.0 --port $PORT"